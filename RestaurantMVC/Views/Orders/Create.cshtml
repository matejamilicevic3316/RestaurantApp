@model RestaurantMVC.Models.CreateOrderModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>
@if (TempData["error"] != null)
{
    <p class="text-danger">@TempData["error"]</p>
}
<h4>CreateOrderModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.OrderRequest.IdArticle" class="control-label"></label>
                <select asp-for="@Model.OrderRequest.IdArticle" class="form-control">
                    @foreach (var type in Model.Articles)
                    {
                        <option value="@type.Id">@type.Name</option>
                    }
                </select>
                <span asp-validation-for="@Model.OrderRequest.IdArticle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.OrderRequest.IdWaiter" class="control-label"></label>
                <select asp-for="@Model.OrderRequest.IdWaiter" class="form-control">
                    @foreach (var type in Model.Waiters)
                    {
                        <option value="@type.Id">@type.FirstName @type.LastName</option>
                    }
                </select>
                <span asp-validation-for="@Model.OrderRequest.IdWaiter" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.OrderRequest.IdTable" class="control-label"></label>
                <select asp-for="@Model.OrderRequest.IdTable" class="form-control">
                    @foreach (var type in Model.Tables)
                    {
                        <option value="@type.Id">@type.Name - @type.Sector</option>
                    }
                </select>
                <span asp-validation-for="@Model.OrderRequest.IdTable" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

